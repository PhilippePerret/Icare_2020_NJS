h2 Préférences

div.explication.
  Vous pouvez régler dans cette section toutes vos préférences concernant votre travail et l'atelier en général.

div.flex2
  div.col
    form(id="preferences_form" method="POST" action="/bureau/preferences/save")
      div.preference
        input(type="checkbox")
        label Me prévenir lorsque j'arrive à échéance de mon travail.
      div.preference
        | Après mon identification rejoindre
        |
        //- 
          TODO
          Utiliser REDIRECTIONS_AFTER_LOGIN pour définir les possibilités
        
        select(name="routeAfterLogin")
          - console.log("User.REDIRECTIONS_AFTER_LOGIN = ", User.REDIRECTIONS_AFTER_LOGIN) 
          for pref, prefid in User.REDIRECTIONS_AFTER_LOGIN
            if pref.admin != true || User.isAdmin
              option(value=prefid)= pref.hname
  
  div.col
    | La seconde colonne, pour voir comment ça va se passer
